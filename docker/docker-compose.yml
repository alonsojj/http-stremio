services:
  app:
    working_dir: /app
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "6222:6222" 
    volumes:
      - ../certs:/app/certs:ro
    restart: unless-stopped
    env_file:
      - ../.env